{% extends 'SmartkillWebBundle::layout.html.twig' %}
{% form_theme edit_form 'SmartkillWebBundle:Form:horizontal.html.twig' %}

{% block title %}SmartKill - Mecze{% endblock %}

{% block content %}

	<div class="block">
		<h2>Edytuj mecz</h2>
		
		<form action="{{ path('match_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
		    {{ form_widget(edit_form) }}
		    <p>
		    	<button class="btn btn-primary btn-large pull-right" type="submit">Edytuj</button>
		    </p>
		</form>
		
		<ul class="record_actions">
		    <li>
		        <a href="{{ path('match') }}">
		            Powrót do listy
		        </a>
		    </li>
		    <li>
		        <form action="{{ path('match_delete', { 'id': entity.id }) }}" method="post">
		            {{ form_widget(delete_form) }}
		            <button type="submit">Delete</button>
		        </form>
		    </li>
		</ul>
	</div>

{% endblock %}